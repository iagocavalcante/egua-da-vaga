// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "dev.iagocalvcante"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
